#include <stdio.h>
#include <string.h>
int main (void) 
{

int age;
float gpa;
char grade_letter;
char password [50];

printf("Enter age: ");
scanf(" %d", &age);
printf("Enter GPA: ");
scanf(" %f", &gpa);
printf("Enter grade letter A or B or C: ");
scanf(" %c", &grade_letter);
printf("Enter password: ");
scanf(" %s", password);

{ 
    if (age < 18) {
   printf("You are a minor student.\n");
   }

   else   {
   printf("You are an adult old.\n");
   }

  
}

{
if ( grade_letter == 'A' || grade_letter == 'B' || grade_letter == 'a' || grade_letter == 'b')
 {
printf("Good job!\n");
 }

 else {
    printf("You need to work harder!\n");
 }

}


{
if (gpa >= 3.5) {printf("Excellent performance!");}
else                          { printf("Warning: Low GPA");}
}



{
if (strcmp(password, "jikar123") == 0) {
printf("\nAccess granted!"); 
}
else {
printf("\nWrong password!"); 
}
}

return 0;
}
